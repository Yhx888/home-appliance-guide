# 🏗️ 全国家电选购指南 — 目标模式执行提示词

> 复制下面全部内容，作为新目标模式对话的提示词。你只需要给模型这个提示词 + 允许它读文件就行。
> 源文件路径：`C:\HOME\Project\home-appliance-guide\`

---

```
================================================================================
【目标模式 · 开始执行】
================================================================================

你现在的任务是：将 C:\HOME\Project\home-appliance-guide\ 这个纯静态单页 HTML
（全国家电选购指南，17 品类 + 8 专题）改造成一个前端+后端完整系统。

用户电脑（Windows）就是服务器，后端跑在 localhost:8000。
务必全自动化执行直至数据精准、所有标准达标，中途不得停止。

先读这些文件熟悉上下文：
  - AGENTS.md     — 项目架构、工具规则、工作流
  - SCHEMA.md     — 17 品类维度定义、数据质量规范
  - index.html    — 现有前端代码（需保持外观不变）

================================================================================
【现有项目状态】
================================================================================

- 唯一起效文件：index.html（~1401 行，HTML+CSS+JS 全内联）
- 数据全部硬编码在 HTML 表格中，纯文本，无结构化数值
- 前端交互：switchTab() / toggleCard() / doSearch() 三个 JS 函数
- 17 品类 id: cat-1 ~ cat-17（具体品类名见 index.html 导航栏）
- 8 专题 id: topic-1 ~ topic-8
- 品类维度差异极大（每个品类 dim-table 结构不同）
- 无构建工具、无依赖、无 npm、纯静态

================================================================================
【完整架构】
================================================================================

┌─ localhost:8000 ──────────────────────────────────────┐
│  FastAPI 后端 serve index.html + JSON API              │
│  SQLite 数据库 → backend/app.db                        │
│  数据管道（独立模块按需运行）                           │
└────────────────────────────────────────────────────────┘

后端目录结构（AGENTS.md 中有完整说明）：
backend/
  requirements.txt, main.py, database.py, schemas.py
  routers.py, scorer.py, seed_data.py
  scrapers/jd.py, scrapers/manufacturer.py, scrapers/verify.py

================================================================================
【数据库模型（详见 AGENTS.md + SCHEMA.md）】
================================================================================

核心表：categories、dimensions（每品类独立维度）、products（JSON 存维度值）、
data_sources、data_points（单源原始数据，用于校对）

================================================================================
【品牌覆盖（每品类 ≥8 品牌，从低端到高端）】
================================================================================

1. 抽油烟机: 森太·苏泊尔·名气·美的·华帝·海尔·老板·方太·西门子
2. 燃气灶: 苏泊尔·万和·美的·华帝·海尔·老板·方太·林内·能率
3. 蒸烤箱: 德普·美的·老板·方太·凯度·西门子·松下
4. 洗碗机: 慧曼·华帝·美的·海尔·老板·方太·西门子·卡萨帝·美诺
5. 净水器: 小米·海尔·美的·沁园·安吉尔·A.O.史密斯·霍尼韦尔
6. 冰箱: 华凌·小米·容声·美的·海尔·西门子·松下·卡萨帝
7. 消毒柜: 康宝·美的·海尔·老板·方太
8. 中央空调: 小米·统帅·海尔·美的·格力·日立·大金·三菱电机
9. 新风系统: 造梦者·远大·德普莱太·松下·霍尼韦尔·AIRMX
10. 挂机/柜机: 奥克斯·TCL·华凌·小米·海尔·美的·格力·卡萨帝
11. 扫地机器人: 小米·科沃斯·石头·云鲸·追觅·添可
12. 空气净化器: 小米·美的·352·宫菱·IAM·霍尼韦尔·IQAir
13. 智能马桶: 九牧·箭牌·海尔·恒洁·松下·东芝·TOTO
14. 热水器: 万和·美的·海尔·卡萨帝·林内·能率·A.O.史密斯
15. 洗衣机: 海信·美的·小天鹅·海尔·松下·东芝·西门子·卡萨帝·美诺
16. 电视机: 雷鸟·小米·创维·TCL·海信·LG·三星·索尼
17. 集成灶+垃圾处理器+管线机: 帅丰·森歌·亿田·火星人·美大·贝克巴斯·爱适易·美的
    总产品数目标：≥ 500

================================================================================
【工具使用规则 — 重要】
================================================================================

禁止使用：webfetch
可以使用：
  ✅ AnySearch        — 搜索品牌热门产品型号、查找商品参数、比价调研
  ✅ Kimi WebBridge   — 浏览器交互：打开网页、点击、截图、滚动
  ✅ playwright       — 自动浏览器截图、批量页面操作
  ✅ @observer        — 对截图进行视觉分析，提取结构化参数

数据采集链路：AnySearch 调研 → Kimi WebBridge/playwright 打开页面 → 读取 HTML
→ 若失败则截图+@observer 提取。禁止使用 webfetch。

================================================================================
【API 接口】
================================================================================

GET  /api/categories
  → 品类列表 + 每品类的维度定义

GET  /api/categories/{slug}/products
  ?brands=美的,海尔,方太          品牌筛选
  &price_min=1000&price_max=5000  价格区间
  &sort_key=风量_m3               单维度排序（配合 sort_dir）
  &sort_dir=desc
  &weights={"风量_m3":80,"静压_Pa":60}  加权排序模式
  &page=1&page_size=50
  → 产品列表 + 各维度标准化得分 + 综合得分

GET  /api/products/{id}
  → 单产品详情 + 关联 data_sources

GET  /api/categories/{slug}/dimensions
  → 某品类维度定义完整列表

================================================================================
【加权排序算法（scorer.py）】
================================================================================

综合得分 = Σ(维度_i 标准化得分 × 权重_i) / Σ(权重_i)

float 标准化:
  higher_better: score = (v - min) / (max - min) × 100
  lower_better:  score = (max - v) / (max - min) × 100

enum 标准化: 预设映射（一级=100, 二级=70, 三级=40, 四级=10）
bool 标准化: true=100, false=0

================================================================================
【前端改造要求】
================================================================================

原则：保留现有全部 CSS、布局、卡片结构、导航、搜索。
改动限：

1. 在每个品类 dim-table 上方插入 <div class="filter-panel">
   - 排序方式切换按钮（单维度 / 加权）
   - 维度选择下拉（单维度模式）
   - 权重滑块（加权模式，每维度一行，带中文标签）
   - 品牌多选（标签样式）
   - 价格区间滑条
   - [应用筛选] [重置] 按钮

2. JS 新增函数（加在现有 <script> 里或单独 script 块）：
   - async function fetchCategories() → GET /api/categories
   - async function fetchProducts(slug, filters) → GET /api/.../products
   - function renderDimTable(products, dims) → 动态绘制表格
   - function applyFilters() → 收集状态 → fetch → 渲染

3. 搜索功能 doSearch() 保持本地搜索不变
4. 切品类时自动请求对应 API 更新数据

================================================================================
【数据采集管道】
================================================================================

数据采集使用「AnySearch + Kimi WebBridge + 截图兜底」三层策略：

Step 0 — AnySearch 调研：
  对每个品牌+品类，先用 AnySearch 搜索热门型号和参数概览
  例如：AnySearch 搜索"方太 抽油烟机 2026 热门型号"
  收集型号列表，作为爬取指引

Step 1 — Kimi WebBridge / playwright + HTML 解析：
  Kimi WebBridge 或 playwright 打开京东搜索页或产品详情页
  尝试直接读取页面 HTML 提取规格参数表
  映射到维度定义，写入 data_points（source=jd_html）

Step 2 — 截图多模态兜底：
  HTML 解析失败 → Kimi WebBridge/playwright 截图规格表区域
  @observer 视觉分析提取参数
  写入 data_points（source=jd_vision）

厂商官网爬虫（补充）：
  用 Kimi WebBridge/playwright 打开官网 → 同样先 HTML 解析，失败则截图+@observer

注意事项：
  - AnySearch 使用中文关键词搜索当前年份热门型号
  - Kimi WebBridge 每次操作间隔 1-3 秒
  - 遇到反爬/验证码则跳过该商品，记录日志
  - 不要突破反爬机制

================================================================================
【多方校对引擎（scrapers/verify.py）】
================================================================================

校对分两个层面：数值核对 + 视觉核验。

【数值核对】
对每 (product, dimension_key) 分组所有 data_points：

- 多来源且值一致（偏差 <15%）→ 置信度 ≥0.9，写入 products.dimensions
- 多来源但偏差 >20% → 标记 "需人工核查"
- 单来源 → 置信度 0.5，暂写入但标记 "待补充"

【视觉核验（多模态交叉确认）】
对有标记 "待补充" 或 "需人工核查" 的产品：

  1. Kimi WebBridge / playwright 打开该产品在京东/厂商官网的页面
  2. 截图规格参数区域
  3. 用 @observer（视觉分析）提取参数值
  4. 将新提取的值作为新的 data_points 加入
  5. 重新运行数值核对，看是否能消解差异

【运行 verifier.py】
  1. 遍历所有未核查 data_points
  2. 分组计算共识值和置信度
  3. 对低置信度项触发视觉核验（Kimi WebBridge/playwright + @observer）
  4. 写入 products.dimensions JSON
  5. 输出数据质量报告（覆盖率、差异列表、置信度分布）

================================================================================
【数据完成标准 —— 必须全部满足才能结束】
================================================================================

□ 品类数 = 17（对应 cat-1 ~ cat-17）
□ 每个品类至少 8 个品牌（消毒柜/新风等小众品类可以 5-6 个）
□ 总产品数 ≥ 500
□ 每个产品关键维度填充率 ≥ 80%
□ 每个产品至少有 2 个独立数据来源
□ 核心品牌（美的、海尔、格力等）100% 覆盖
□ 无 "需人工核查" 的未解决数据项
□ 价格区间与实际市场价偏差 < 30%
□ 所有 API 端点正常响应
□ 前端浏览器打开 http://localhost:8000 所有品类可见，
   筛选排序面板可操作、表格数据动态刷新
□ 搜索功能正常

================================================================================
【实施顺序（严格按此顺序，不得跳跃）】
================================================================================

Phase 1 — 后端骨架
  1. 创建 backend/ 目录结构 + requirements.txt（fastapi uvicorn sqlalchemy httpx parsel）
  2. 安装依赖（pip install -r requirements.txt）
  3. 创建 database.py（SQLAlchemy 引擎 + 全部表模型）
  4. 创建 main.py（FastAPI 应用 + CORS + 静态文件 serve index.html）
  5. 创建 schemas.py（Pydantic 模型）
  6. 创建 routers.py（品类列表路由）
  7. 测试启动：uvicorn backend.main:app，访问 http://localhost:8000 能看到页面

Phase 2 — 维度定义 + 数据迁移
  8. 参考 SCHEMA.md，在 seed_data.py 中定义所有 17 品类的维度
  9. 从 index.html 解析现有表格数据
  10. 写入数据库（categories + dimensions + products）
  11. 验证数据完整

Phase 3 — API + 排序引擎
  12. 实现 scorer.py（加权评分算法）
  13. 完善 routers.py（产品查询、排序筛选、维度定义接口）
  14. 测试各 API 接口

Phase 4 — 前端改造
  15. 修改 index.html：新增 filter-panel HTML 结构
  16. 新增 filter-panel CSS 样式
  17. 新增 JS：API 客户端 + 动态渲染 + 筛选状态管理
  18. 测试筛选排序交互

Phase 5 — 数据采集 + 多模态校对（最耗时）
  19. 确定每个品类当前已有的产品数据缺口
  20. 用 AnySearch 搜索每个品牌的热门产品型号
  21. 用 Kimi WebBridge / playwright 打开京东产品页，提取规格参数
     HTML 解析优先，失败则截图+@observer 兜底
  22. 将采集到的数据写入 data_points
  23. 用 Kimi WebBridge / playwright 打开厂商官网补充数据
  24. 实现 verify.py 多方校对引擎（含视觉核验回路）
  25. 运行校对 → 对低置信度项触发视觉核验 → 修复数据
  26. 循环直至通过完成标准

Phase 6 — 最终验证
  27. 逐项检查"数据完成标准"列表
  28. 所有品类在浏览器中手动确认
  29. 服务保持运行，退出前报告最终状态

================================================================================
【重要注意事项】
================================================================================

1. 所有代码写中文注释
2. 依赖安装在 backend/requirements.txt，用 pip install -r
3. SQLite 数据库文件在 backend/app.db，数据库文件不要提交 git
4. 不要修改现有 index.html 的 CSS 主题、导航、卡片折叠、搜索已有功能
5. 前端新增内容加在现有结构的合理位置，不要删除已有内容
6. 爬虫遇到反爬不要硬突破，跳过该产品，记录日志
7. 数据校对阶段发现差异时，优先信任京东 HTML 数据，其次是京东视觉数据
8. 最后浏览器访问 http://localhost:8000 确认一切正常
9. 完成时输出数据库统计（品类数、产品数、数据来源数、覆盖率和完成度）
10. 禁止使用 webfetch，浏览器操作用 Kimi WebBridge 或 playwright

【Git 管理规范】
每个 Phase 完成后必须 git commit，提交信息示例：
  - Phase 1 完成：`Phase 1: 后端骨架搭建完成`
  - Phase 4 完成：`Phase 4: 前端筛选面板+动态渲染`
  - 数据更新：`新增抽油烟机品类数据（9品牌45产品）`
  - Bug 修复：`修复xx问题`
提交前运行 `git status` 确认只包含本阶段文件，数据库文件和 __pycache__ 不提交。
提交格式：逐个文件 `git add <文件>`，然后 `git commit -m "信息"`。
完成后 push（注意 git 代理配置 127.0.0.1:7897）。

================================================================================
【启动命令】
================================================================================

uvicorn backend.main:app --reload --host 0.0.0.0 --port 8000
```
