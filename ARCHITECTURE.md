# 🏗️ 全国家电选购指南 — 前端+后端完整架构计划

> 最后更新：2026-07-31
> 本文件为整体设计文档，不参与执行。执行提示词见 PLAN.md。

---

## 一、架构总览

```
┌─ 你的电脑（Windows）─ 同时充当 server ──────────────────────┐
│                                                              │
│  ┌──────────────────────────────────────────────────────┐    │
│  │  FastAPI 后端 (localhost:8000)                        │    │
│  │  ├── 提供 JSON API + 同时 serve index.html           │    │
│  │  ├── 静态文件 → C:\HOME\...\index.html               │    │
│  │  └── 数据库 → backend/app.db (SQLite)                │    │
│  └──────────────┬───────────────────────────────────────┘    │
│                 │                                            │
│  ┌──────────────▼───────────────────────────────────────┐    │
│  │  浏览器 → http://localhost:8000                       │    │
│  │  index.html (前端，保持现有外观)                       │    │
│  │  + JS 从 API 拉数据动态渲染表格                        │    │
│  └──────────────────────────────────────────────────────┘    │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐    │
│  │  数据管道（独立模块，按需运行）                         │    │
│  │  jd_scraper.py → 京东商品参数采集                      │    │
│  │  manufacturer_scraper.py → 厂商官网采集                │    │
│  │  verifier.py → 多源校对引擎                            │    │
│  └──────────────────────────────────────────────────────┘    │
└──────────────────────────────────────────────────────────────┘
```

---

## 二、品牌覆盖计划（从低价到高端全覆盖）

每品类 **8-10 个品牌**，每品牌 **3-6 款产品**，总计 **~536 款产品**。

| # | 品类 | 品牌覆盖（←低端 中端 高端→） | 约产品数 |
|:-:|------|------------------------------|:--------:|
| 1 | 抽油烟机 | 森太·苏泊尔·名气·美的·华帝·海尔·老板·方太·西门子 | 9×5=45 |
| 2 | 燃气灶 | 苏泊尔·万和·美的·华帝·海尔·老板·方太·林内·能率 | 9×4=36 |
| 3 | 蒸烤箱 | 德普·美的·老板·方太·凯度·西门子·松下 | 7×4=28 |
| 4 | 洗碗机 | 慧曼·华帝·美的·海尔·老板·方太·西门子·卡萨帝·美诺 | 9×4=36 |
| 5 | 净水器 | 小米·海尔·美的·沁园·安吉尔·A.O.史密斯·霍尼韦尔 | 7×4=28 |
| 6 | 冰箱 | 华凌·小米·容声·美的·海尔·西门子·松下·卡萨帝 | 8×5=40 |
| 7 | 消毒柜 | 康宝·美的·海尔·老板·方太 | 5×3=15 |
| 8 | 中央空调 | 小米·统帅·海尔·美的·格力·日立·大金·三菱电机 | 8×4=32 |
| 9 | 新风系统 | 造梦者·远大·德普莱太·松下·霍尼韦尔·AIRMX | 6×3=18 |
| 10 | 挂机/柜机 | 奥克斯·TCL·华凌·小米·海尔·美的·格力·卡萨帝 | 8×5=40 |
| 11 | 扫地机器人 | 小米·科沃斯·石头·云鲸·追觅·添可 | 6×5=30 |
| 12 | 空气净化器 | 小米·美的·352·宫菱·IAM·霍尼韦尔·IQAir | 7×4=28 |
| 13 | 智能马桶 | 九牧·箭牌·海尔·恒洁·松下·东芝·TOTO | 7×5=35 |
| 14 | 热水器 | 万和·美的·海尔·卡萨帝·林内·能率·A.O.史密斯 | 7×4=28 |
| 15 | 洗衣机 | 海信·美的·小天鹅·海尔·松下·东芝·西门子·卡萨帝·美诺 | 9×4=36 |
| 16 | 电视机 | 雷鸟·小米·创维·TCL·海信·LG·三星·索尼 | 8×5=40 |
| 17 | 集成灶/管 | 帅丰·森歌·亿田·火星人·美大·贝克巴斯·爱适易 | 7×3=21 |
| 18 | 垃圾处理器 | 贝克巴斯·唯斯特姆·爱适易·海尔 (在 cat-17 中子品类) | +4 |
| 19 | 管线机 | 美的·海尔·沁园·A.O.史密斯·安吉尔 (在 cat-17 中子品类) | +5 |
| | **合计** | | **~545** |

---

## 三、数据库设计

### 3.1 ER 模型

```sql
-- 品类表
categories
  id           INTEGER PK
  name         TEXT        -- "抽油烟机"
  slug         TEXT UNIQUE -- "range-hood"
  icon         TEXT        -- "🍳"
  description  TEXT

-- 品类维度定义表（不同品类维度不同）
dimensions
  id              INTEGER PK
  category_id     INTEGER FK → categories
  dim_key         TEXT        -- "风量_m3" (英文字段KEY)
  label           TEXT        -- "风量" (中文显示名)
  dim_type        TEXT        -- "float" | "enum" | "bool" | "text"
  unit            TEXT        -- "m³/min" (单位)
  higher_better   BOOLEAN     -- true=越大越好, false=越小越好
  default_weight  INTEGER     -- 默认推荐权重 0-100
  sort_order      INTEGER     -- 在表格中的显示顺序
  enum_values     TEXT JSON   -- ["一级","二级","三级"] (enum类型用)

-- 产品表
products
  id              INTEGER PK
  category_id     INTEGER FK → categories
  brand           TEXT        -- "方太"
  model           TEXT        -- "JCD10TA"
  price_low       REAL        -- 最低价（元）
  price_high      REAL        -- 最高价（元）
  dimensions      TEXT JSON   -- {"风量_m3": 23, "静压_Pa": 1000, ...}
  rating          REAL        -- 综合评分 0-100
  summary         TEXT        -- 一句话推荐理由
  source_url      TEXT        -- 数据来源URL

-- 数据来源表
data_sources
  id              INTEGER PK
  platform        TEXT        -- "jd" | "manufacturer" | "xhs" | ...
  platform_label  TEXT        -- "京东" | "厂商官网"
  url             TEXT
  collected_at    TIMESTAMP

-- 数据点表（多方校对用）
data_points
  id              INTEGER PK
  product_id      INTEGER FK → products
  dimension_key   TEXT        -- "风量_m3"
  source_id       INTEGER FK → data_sources
  raw_value       TEXT
  numeric_value   REAL
  confidence      REAL        -- 置信度 0-1
  verified        BOOLEAN
```

### 3.2 维度设计示例（抽油烟机）

| dim_key | label | type | unit | higher_better | weight |
|---------|-------|------|------|:-------------:|:------:|
| 价格区间_低 | 最低价 | float | 元 | false | 70 |
| 风量_m3 | 风量 | float | m³/min | true | 90 |
| 静压_Pa | 最大静压 | float | Pa | true | 85 |
| 噪音_dB | 噪音 | float | dB | false | 60 |
| 能效等级 | 能效等级 | enum | — | — | 50 |
| 保修_年 | 保修 | float | 年 | true | 40 |
| 满意度评分 | 满意度 | float | 分 | true | 75 |

每个品类独立注册各自的维度，维度集完全不同。

---

## 四、排序筛选系统

### 4.1 API

```
GET /api/categories                           → 品类列表+维度定义
GET /api/categories/{slug}/products           → 产品列表+排序筛选
  ?brands=美的,海尔,方太
  &price_min=1000&price_max=5000
  &sort_key=风量_m3&sort_dir=desc
  &weights={"风量_m3":80,"静压_Pa":60}
  &page=1&page_size=50
GET /api/products/{id}                        → 单产品详情+来源
GET /api/categories/{slug}/dimensions         → 某品类维度定义
```

### 4.2 加权排序算法

```
综合得分 = Σ(维度_i 标准化得分 × 权重_i) / Σ(权重_i)

float 标准化:
  higher_better: score = (v - min) / (max - min) × 100
  lower_better:  score = (max - v) / (max - min) × 100

enum 标准化: 如 一级=100, 二级=70, 三级=40, 四级=10
bool 标准化: true=100, false=0
```

### 4.3 筛选组合

品牌多选 + 价格滑条 + 单维度排序 / 加权综合排序 → 一次 API 调用。

---

## 五、前端改造

保留所有 CSS、布局、卡片结构。只改：

**新增**：每个品类 dim-table 上方插入筛选面板 `filter-panel`
- 排序方式切换（单维度 / 加权）
- 维度选择下拉 + 升降序
- 权重滑块组
- 品牌复选框 + 价格区间的简化选择
- [应用筛选] [重置] 按钮

**修改**：
- 静态 dim-table → JS `renderTable()` 动态渲染
- 推荐型号表也走 API
- 搜索增强为可联动筛选项

**JS 新增** (~3 个函数)：
- `fetchProducts(slug, filters)` — API 调用
- `renderDimTable(products, dims)` — 表格渲染
- `applyFilters()` — 状态管理

---

## 六、数据采集管道

### 6.1 双层采集策略（HTML 解析 + 多模态截图兜底）

| 层 | 方法 | 适用场景 | 成本 |
|:--:|------|---------|:----:|
| L1 HTML 解析 | httpx + parsel 提取规格参数表 | 参数表结构规整 | 低 |
| L2 多模态截图 | playwright / Kimi WebBridge 截图 → @observer 视觉提取 | HTML 解析失败、参数在图片/复杂渲染中 | 中高 |

流程：
```
取一个产品 → 尝试 HTML 解析
  ├─ 成功 → 参数写入 data_points（source=jd_html）
  └─ 失败 → playwright/Kimi WebBridge 打开页面 → 截图参数区域
            → @observer 视觉分析提取数值
            → 参数写入 data_points（source=jd_vision）
```

### 6.2 数据来源

| 来源 | 策略 | 频率 |
|------|------|:----:|
| **京东** | L1 HTML 优先 + L2 截图兜底 | 一次全量 + 按需增量 |
| **厂商官网** | 方太/老板/美的/海尔/格力等官网规格页 | 一次全量 |
| **社会媒体** | 现有页面已有数据入库，半人工摘录 | 数据校对阶段 |
| **黑猫投诉** | 公开页面获取投诉量/回复率 | 一次性 |

### 6.3 防爬策略

- 随机 User-Agent
- 每请求间隔 1-3 秒
- 失败重试 3 次（指数退避）
- 429 响应暂停 60 秒
- 不突破反爬机制，L1/L2 都失败则跳过该产品

---

## 七、多方核验校对引擎

### 7.1 核对方法（数值 + 视觉双通道）

| 通道 | 方法 | 用途 |
|:----:|------|------|
| **数值核对** | 多来源数据点分组统计 | 批量快速核对 |
| **视觉核验** | 截图 + @observer 视觉分析 | 对低置信度/单来源项二次确认 |

### 7.2 置信度模型

```
对每个 (product, dimension_key) 分组所有 data_points：

样本数 ≥ 2 且标准差/均值 < 15%  → 置信度 0.9+，标记 "已确认"
样本数 ≥ 2 但偏差 > 20%         → 标记 "需人工核查"
单来源                          → 置信度 0.5，标记 "待补充"

对标记 "待补充" 或 "需人工核查" 的项：
  → 自动触发视觉核验：
    浏览器打开产品页 → 截图参数区 → @observer 提取
  → 新数据点加入 → 重新计算置信度
```

### 7.3 共识值写入流程

```
1. 遍历某品类所有 data_points
2. 按 (product, dimension_key) 分组
3. 计算共识值（均值/中位数）和置信度
4. 低置信度项触发视觉核验回路
5. 共识值写入 products.dimensions JSON
6. 生成数据报告（覆盖率、差异标记列表、置信度分布）
```

### 7.3 数据完成标准（全部满足才视为完成）

```
✅ 每品类产品数 ≥ 品牌数 × 3
✅ 每产品关键维度填充率 ≥ 80%
✅ 每产品至少有 2 个独立来源
✅ 全品类关注核心品牌（美的、海尔、格力等）100% 覆盖
✅ 无标记"需人工核查"的未解决项
✅ 价格区间与实际市场价偏差 < 30%
```

---

## 八、实施阶段

| 阶段 | 内容 | 说明 |
|:----:|------|------|
| **1** | 后端骨架：FastAPI + SQLite + 模型定义 + 静态文件 serve | ~8 步 |
| **2** | 维度定义：17 品类逐一注册维度集（~120+ 维度） | ~5 步 |
| **3** | 数据迁移：现有 index.html 数据解析入库 | ~4 步 |
| **4** | API + 排序引擎：评分算法、筛选逻辑、路由 | ~6 步 |
| **5** | 前端改造：筛选面板 UI + 动态渲染 JS | ~8 步 |
| **6** | 京东爬虫：通用模块 + 各品类适配 | ~10 步 |
| **7** | 厂商爬虫：焦点厂商规格页 | ~5 步 |
| **8** | 全品类爬取运行 | 耗时阶段 |
| **9** | 多方校对 + 数据验证：差异分析 + 修复 | ~5 步 |
| **10** | 前端打磨：loading/error/empty 态 | ~4 步 |
| **11** | 验证：浏览器打开 `http://localhost:8000`，所有品类完美工作 | ~2 步 |

---

## 九、质量目标

| 指标 | 目标值 |
|------|:------:|
| 每品类品牌数 | ≥8 |
| 总产品数 | ≥500 |
| 每产品维度填充率 | ≥80% |
| 双源覆盖比例 | ≥70% |
| 价格偏差 | <30% |
| API 排序响应 | <1 秒 |
